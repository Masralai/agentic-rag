import { EmptyState } from "@/components/empty-state";

export default function NodesPage() {
  return (
    <EmptyState
      title="Select a node"
      description="Choose a node from the sidebar or create a new one."
    />
  );
}
