export default function NodesPage() {
  return (
    <div className="flex-1 flex items-center justify-center text-text-muted">
      <div className="text-center">
        <p className="text-2xl font-bold tracking-tight">Select a node</p>
        <p className="text-sm mt-1">Choose a node from the sidebar or create a new one</p>
      </div>
    </div>
  );
}
